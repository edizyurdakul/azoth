{
	"name": "@azoth/tracker",
	"module": "src/index.ts",
	"type": "module",
	"private": true,
	"scripts": {
		"build": "bun build src/index.ts --outfile ../ingestion/public/tracker.min.js --minify --target=browser",
		"test": "bun test --pass-with-no-tests",
		"typecheck": "tsc --noEmit"
	},
	"devDependencies": {
		"@types/bun": "latest"
	},
	"peerDependencies": {
		"typescript": "^5"
	}
}
